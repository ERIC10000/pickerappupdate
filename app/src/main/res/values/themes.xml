<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KenWaPWA" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color (Jungle Green) -->
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorPrimaryVariant">@color/primary_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color (Orange) -->
        <item name="colorSecondary">@color/secondary_orange</item>
        <item name="colorSecondaryVariant">@color/secondary_orange_dark</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color (This makes the top bar Green everywhere) -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Light status bar icons if the green is dark enough, otherwise remove this -->
        <!-- <item name="android:windowLightStatusBar" tools:targetApi="m">true</item> -->
    </style>
</resources>